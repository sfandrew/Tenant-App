<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-2">
    <%= f.label :role %><br>
    <%= f.select :role, options_for_select([['Admin','admin']]),  {:include_blank => true}, {:class => 'form-control'} %>
    <%= f.submit :class => 'btn btn-success' %>
  </div>
<% end %>
