<% if user_signed_in? %>
	<div id="feedback-widget">
		<%= button_tag 'Feedback', :class => 'btn btn-success', "data-toggle" => "modal", "data-target" => "#feedbackModal" %>
	</div>

	<%= render partial: 'feedbacks/form' %>
<% end %>